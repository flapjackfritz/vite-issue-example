import { BrowserRouter } from 'react-router-dom';
import {Redirect} from 'react-router';
import bar from "./dep2";

export const stub = () => {
  bar();
  BrowserRouter;
  Redirect;
}

export default stub;