import all from 'react-router-dom';
import foo from 'react-router';
import {
  AsyncResponseResolverReturnType,
  RestRequest,
  MockedResponse,
  ResponseComposition,
  rest,
  RestContext,
} from "msw";

const stub = () => {
}

export default stub;